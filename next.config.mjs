/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['res.cloudinary.com',"unsplash.com", "plus.unsplash.com","images.remotePatterns","img.freepik.com"],
      },
};

export default nextConfig;
